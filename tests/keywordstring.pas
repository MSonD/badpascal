PROGRAM identif (input);
(*** no va esto *)
VAR a, b: integer;
   gg: integer;
BEGIN
  a:=5;
  b:=a +3;
  IF a >= 18 THEN
  BEGIN
    b := 40
  END;; (*la sintaxis de else es obtusa por el cambio*)
  writeln ('salida  ') ;
  IF a = 60 THEN
	x := 0 ; ; 
  FOR x:= 0xDEAD DOWNTO ident + 8 DO
  BEGIN
	writeln('')
  END;
  IF a = b THEN
    x:=90; ;
  IF g = ib THEN
    x := 90; ;
  REPEAT K := k + 1 UNTIL K > 9;
  REPEAT K := k + 1 UNTIL K > 9;
  FOR x:= 0xDEAD DOWNTO i DO
  BEGIN
	writeln('')
  END;
  
  FOR x:= 90 DOWNTO 0 DO
    writeln('')
END .
